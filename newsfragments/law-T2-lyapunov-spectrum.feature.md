**Law T2 — Full Lyapunov spectrum.** New `core/physics/lyapunov_spectrum.py` co-integrates the variational flow `Q̇ = J(x) Q` with the state ODE under a midpoint integrator and reorthonormalises via `lax.fori_loop` + reduced QR. Returns `LyapunovReport` with the descending-sorted spectrum, log-growth diagnostics, and the final state/frame. Constitutional invariants `INV-LY1` (linear-flow recovery), `INV-LY2` (Hamiltonian sum-zero), `INV-LY3` (fail-closed contract). Lorenz-63 recovers the published reference `(0.9056, 0, -14.5723)` within 10 % at `T = 1000`. Bit-identical determinism (INV-HPC1) verified.
