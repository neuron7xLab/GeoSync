**Law T7 — Operational pinning control of chaos.** New `runtime/pinning_control.py` ships pure-NumPy primitives implementing Wang-Chen (2002) pinning control: `graph_laplacian`, `algebraic_connectivity` (Fiedler λ_2), `pinning_gain_margin`, `select_pinning_set` (greedy O(N²·k) selection maximising λ_2(L + Γ_P) under hard cap, returns `PinningReport(status, pinned_indices, gain_margin, iterations)` with `PinningStatus` ∈ {SUFFICIENT, INSUFFICIENT}), `pinning_step` (one explicit-Euler step of `ẋ = −L·x − Γ_P·(x − x_target)`). Constitutional invariants `INV-PIN1` (returned P satisfies λ_2(L + Γ_P) > ε_pin OR INSUFFICIENT — fail-closed), `INV-PIN2` (contractive in linearised regime; negative control proves contraction is from pinning, not the Laplacian), `INV-PIN3` (A never mutated — topology preservation), `INV-PIN4` (every contract violation → ValueError). Self-contained physics; no external ML in the control loop.
