**Law T5 — Landauer-budgeted predictability horizon.** New `core/physics/predictability_horizon.py` ships pure-functional building blocks for the Lorenz (1969) horizon and the Landauer (1961) information-cost floor: `predictability_horizon` (algebraic τ from λ_1, δ_0, δ_tol), `landauer_min_initialisation_energy` (k_B·T·ln(Δ/δ_0)), `landauer_min_initial_precision` (inverse), and the master `predictability_horizon_under_budget` which clamps τ by a hard energy budget and returns `HorizonReport(tau, delta_0, delta_0_min_landauer, energy_required_J, saturated_budget)`. Constitutional invariants `INV-TAU1` (Lorenz formula, +∞ for λ_1 ≤ 0, halves under doubled λ_1), `INV-TAU2` (Landauer floor exact; round-trip to 1e-10 in float64-representable regime; over-budget δ_0 → ValueError), `INV-TAU3` (every contract violation → ValueError). Pure functional; identical inputs ⇒ identical outputs (INV-HPC1 compat).
