Closed two P0 invariant gaps surfaced by a repo consistency audit. **INV-DRO5 fail-closed bug**: `derive_gamma` was silently accepting NaN/Inf/constant/short inputs despite the catalog mandating `ValueError` — now enforced via a one-line `_validate` call, no production caller affected (default window is 512 samples, well above the 64-sample floor). **INV-DRO2 coverage**: 13 cases pin the risk-scalar's `[0, 1]` bound, Lipschitz-1 property, and peak at γ=1. Full audit ships as a frozen 2026-05-03 benchmark under `docs/reports/`.
