**Law T1 — Kuramoto-Ricci sync-onset boundary.** New `core/kuramoto/kuramoto_ricci_engine.py` ships the JAX-pure Kuramoto network on Ollivier-Ricci edge weights with the analytic Restrepo-Ott-Hunt boundary `Φ(K, γ, A) = K · λ_max(A) − 2γ`. Public surface: `phase_transition_boundary` (returns `BoundaryReport`), `ricci_to_adjacency` (filters anti-correlated κ < 0 edges), `kuramoto_ricci_rhs` (factory for T2 estimator), `kuramoto_ricci_step` (midpoint RK2), `kuramoto_ricci_trajectory` (`lax.scan`-fused), `order_parameter`, `coupling_potential`. Constitutional invariants `INV-KR1` (sign(Φ) ⇒ asymptotic ⟨R⟩ regime), `INV-KR2` (variational MLE crosses 0 through Φ = 0, verified via Law T2 spectrum), `INV-KR3` (V monotone non-increasing at ω = 0). Two negative controls (V can rise with ω ≠ 0; K = 0 yields no sync) prove the positive claims are informative.
