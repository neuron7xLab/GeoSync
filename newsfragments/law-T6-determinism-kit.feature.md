**Law T6 — Bit-identical reproducibility kit.** New `core/physics/determinism_kit.py` ships pure-NumPy canonicalisation + hashing utilities making chaotic-trajectory reproducibility a property of bytes, not just numerics: `canonicalize_state` (IEEE-754 normalisation: NaN-patterns collapse, ±0 unify, subnormals flush, little-endian), `state_hash` (SHA-256 over canonical bytes + dtype + shape — sensitive to a single ULP), `trajectory_hash` (per-row digests in order — sensitive to permutation), `ReplayManifest` (sealed audit binding `(integrator_id, seed, dt, n_steps, x0_hash, traj_hash)`), `verify_replay` (re-hash and compare). Constitutional invariants `INV-DET1` (identical canonical input ⇒ identical hash; NaN/±0/subnormal collapses), `INV-DET2` (1-ULP perturbation ⇒ different hash; dtype/shape/order aliasing blocked), `INV-DET3` (every contract violation → ValueError). Substrate for Law T5 — below the predictability horizon τ, replay is bit-identical.
