- Added `runtime.cognitive_bridge` — fail-closed advisory sidecar that wires the deterministic agent state machine to an external LLM host (default target: local OpenClaw gateway over loopback HTTP). Ships with versioned Pydantic protocol, in-memory + httpx transports, seven CB-INV-1..7 invariants (kill-switch / stressed-state preemption, schema validation, timeout collapse, deterministic correlation IDs, advisory-only Layer-2 contract), bounded audit log, 28 tests, and an `examples/cognitive_bridge_demo.py` walkthrough.
