- Added `runtime.cognitive_bridge` — fail-closed advisory sidecar that wires the deterministic agent state machine to an external LLM host (default target: local OpenClaw gateway over loopback HTTP). Ships with versioned Pydantic protocol, in-memory + httpx transports, seven CB-INV-1..7 invariants (kill-switch / stressed-state preemption, schema validation, timeout collapse, deterministic correlation IDs, advisory-only Layer-2 contract), bounded audit log, plus the full 15-stage semantic-sieve cycle (RAW_SIGNAL → CLAIM → CLEAN_CLAIM → INVARIANT → EROSION → FALSIFICATION_CONTRACT → STABILITY → CROSS_DOMAIN → MEANING_FACT → CRYSTALLIZED_FORM → EXECUTABLE_PROTOCOL → RESULT_ARTIFACT → VERIFICATION → AUDIT → KNOWLEDGE_NODE) as a deterministic state machine, the V(O) integral value function with G_v fail-closed gate (V = G_v · ∫(αI + βF + γS + δX + εA + ζR + ηP − λN − μH − νC) dt), and `KnowledgeNode` memory layer with status classification (CORE_FACT / WORKING_HYPOTHESIS / CONTEXTUAL_TOOL / ARCHIVED_ERROR / NOISE / REJECTED). 56 tests, two end-to-end demo scripts.
