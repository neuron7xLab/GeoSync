# SPDX-License-Identifier: MIT
# Production deployment Hydra profile.

defaults:
  - base

name: prod
db_uri: "postgresql://prod:${PROD_DB_PASSWORD:-secure}@host/prod?sslmode=verify-full"
db_tls:
  ca_file: ${PROD_DB_CA_FILE:-/etc/geosync/db/root-ca.pem}
  cert_file: ${PROD_DB_CERT_FILE:-/etc/geosync/db/client.crt}
  key_file: ${PROD_DB_KEY_FILE:-/etc/geosync/db/client.key}
log_level: WARNING
random_seed: 7

data:
  price_csv: /mnt/prod_data/prices.csv

analytics:
  window: 512
  bins: 64
  delta: 0.0025

tracking:
  base_dir: /mnt/prod_runs
