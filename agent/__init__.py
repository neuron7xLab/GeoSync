"""GeoSync Resurrection Agent (SYSTEM_ARTIFACT_v9.0).

Deterministic Cognitive Control Agent for Substrate Revival.

Core thesis (§0):
    OHLC close bars do not contain precursor-grade microstructure.
    Therefore: no precursor claim from close-only topology.

Mission vector:
    DEAD_SUBSTRATE → LIVE_SUBSTRATE → RESEARCH_READY → CAPITAL_REVIEW_READY

The agent is NOT a trader. It is a substrate-control organism that
restores missing physical substrate, routes every missing dependency
into an explicit API-driven task, keeps the system under continuous
calibrated tension, and forbids false precursor narratives until the
substrate is truly alive.

Entry point::

    from agent.main import run_once
    intent = run_once()            # returns ActionIntent (JSON-serialisable)
"""
