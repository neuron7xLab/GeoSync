# Reviewer Brief — GeoSync Canonical Seven (2026-05-08 release candidate)

> Closes audit task T7 of the 9.9 upgrade. Estimated time-to-audit: **30 minutes**.

## What you are reviewing

A **dataset-bounded empirical falsification machine** for the
hypothesis that interbank phase-locking precedes banking-crisis
events. The instrument is **not** an early-warning system, **not**
a trading signal, and has **no measured AUC on real data** at this
release.

## Maximum claim tier

```
HYPOTHESIS / ADVERSARIAL INSTRUMENTATION
END-TO-END EMPIRICAL VALIDATION PENDING
```

## What we ask you to find

You are an adversarial reviewer. We ask you to **break** the
instrument by surfacing:

1. Claim overstatement in any user-facing artefact.
2. A missing null surrogate or null-audit miscalibration.
3. An irreproducible command in `REPRODUCIBILITY_CAPSULE/`.
4. A weak statistical assumption in `bayes_rigorous.py` or
   `falsification.py`.
5. A hidden leakage vector that the six sentinels miss.
6. An API-instability hazard for downstream callers.
7. A test invariant that is too narrow (would pass under a buggy
   implementation).
8. A documentation/code mismatch.
9. A release blocker.

Use `06_REVIEW_FORM.md` to file findings in a structured form.

## Reading order (≤ 30 minutes)

1. `01_CLAIMS.md` — three pages, what the system claims and refuses
   to claim.
2. `02_EVIDENCE_MAP.md` — claim → code → test → falsifier mapping
   (compiled from `research/systemic_risk/claims.yaml`).
3. `03_REPRO_STEPS.md` — how to reproduce locally; expected outputs.
4. `04_KNOWN_LIMITATIONS.md` — what we already know is broken.
5. `05_ATTACK_SURFACE.md` — the threat model.
6. `06_REVIEW_FORM.md` — your structured-finding form.
7. `07_EXPECTED_CRITICAL_FINDINGS.md` — items we already expect to
   appear in your review (for calibration).

## Repository facts (frozen)

| Field | Value |
|---|---|
| Repository | `neuron7xLab/GeoSync` |
| Release tag | `v0.9.9-research-integrity-candidate` (pending) |
| Test count (`tests/research/systemic_risk/`) | 562 |
| Test count (`tests/metamorphic/`) | 25 |
| Test count (`tests/negative_controls/`) | 17 |
| Public API surface | 172 symbols, mapped 1:1 in `public_symbol_matrix.csv` |
| Static checks | mypy --strict, ruff, black — clean |
| Real-data evaluation | **none** — `LIMITATIONS.md § 6` |

## Hard rules of engagement

* **No real-data results in this packet.** Any AUC value you see
  is on synthetic data (`research/systemic_risk/synthetic.py`)
  generated by a deterministic seeded RNG.
* The capsule under `REPRODUCIBILITY_CAPSULE/` is fully reproducible
  from a clean clone.
* If you find a hidden real-data claim, that is a **release
  blocker**. File it under category 1.
