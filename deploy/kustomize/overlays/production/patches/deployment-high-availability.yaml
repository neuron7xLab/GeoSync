apiVersion: apps/v1
kind: Deployment
metadata:
  name: geosync-api
spec:
  replicas: 5
  template:
    metadata:
      annotations:
        security.geosync.dev/log-level: "warn"
        security.geosync.dev/mtls-required: "true"
        prometheus.io/scrape: "true"
        prometheus.io/port: "8001"
      labels:
        environment: production
    spec:
      priorityClassName: geosync-critical
      nodeSelector:
        environment: production
      tolerations:
        - key: environment
          operator: Equal
          value: production
          effect: NoSchedule
      securityContext:
        runAsNonRoot: true
        fsGroup: 2000
      containers:
        - name: geosync-api
          resources:
            requests:
              cpu: "750m"
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
          env:
            - name: GEOSYNC_ENVIRONMENT
              value: production
            - name: GEOSYNC_RATE_LIMIT_TPS
              value: "1200"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: geosync-api
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/name
                    operator: In
                    values:
                      - geosync-api
              topologyKey: kubernetes.io/hostname
