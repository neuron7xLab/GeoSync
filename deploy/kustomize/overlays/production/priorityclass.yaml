apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: geosync-critical
value: 1000000000
preemptionPolicy: PreemptLowerPriority
globalDefault: false
description: "Ensures GeoSync production workloads remain schedulable under cluster pressure."
