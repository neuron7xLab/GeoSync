apiVersion: v1
kind: Service
metadata:
  name: geosync-api
  labels:
    app.kubernetes.io/name: geosync-api
spec:
  selector:
    app.kubernetes.io/name: geosync-api
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: metrics
      port: 8001
      targetPort: metrics
  type: ClusterIP
